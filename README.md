# atv_estagio03_estatisticaAplicada

Primeiro clone o repositório

# Intalando Dependencias:

pipenv install pandas

# Crie um abiente virtual

pipenv shell

# Configure o kernel do notebook para o ambiente virtual criado

vincule o ambiente virtual no notebook e rode no bash:
pip install ipykernel -U --force-reinstall
